{
  "name": "exiftool-mcp-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "exiftool-mcp": "./index.js"
  },
  "type": "module",
  "keywords": [],
  "author": "Vlad Hrybok",
  "license": "ISC",
  "description": "An MCP Server for retrieving EXIF data from images (photos) and videos using ExifTool",
  "dependencies": {
    "execa": "^9.6.0"
  }
}
